import { createFileRoute } from '@tanstack/react-router';
import { EnhancedSwitch } from '../component/MotionAnimation/EnhancedSwitch';

export const Route = createFileRoute('/enhanced-switch')({ component: EnhancedSwitch });
